import moment from 'moment';
import store from '~core/store';
import {{name}}Store from './{{name}}Store';
import * as repository from './repository';

const {{name}}Presenter: any = {...repository};

export default {{name}}Presenter;